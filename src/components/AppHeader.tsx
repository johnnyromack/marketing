// AppHeader now renders the AppSidebar — kept for backward compatibility
// Pages that import AppHeader still work without changes to their imports.
export { AppSidebar as AppHeader } from '@/components/AppSidebar';
